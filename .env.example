# Server
PORT=5000
NODE_ENV=development

# MongoDB connection
MONGODB_URI=ENTER_YOUR_MONGODB_URI

# Frontend origin (used for CORS)
FRONTEND_URL=http://localhost:5173

# Cloudinary (image uploads)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Gmail (for sending approval emails)
# Use an App Password (recommended). Example: EMAIL_USER=you@gmail.com
EMAIL_USER=your.email@gmail.com
EMAIL_PASS=your_gmail_app_password

# Brevo (recommended for Render - free tier)
BREVO_API_KEY=your_brevo_api_key
# Verified sender email for Brevo
EMAIL_FROM=your_verified_sender@yourdomain.com
# Optional: sender name
EMAIL_FROM_NAME=DDKA
# Optional: reply-to address (where recipients should contact you)
EMAIL_REPLY_TO=your_gmail@gmail.com
# Optional: logo shown on all emails (public image URL)
EMAIL_LOGO_URL=https://dhanbadkabaddiassociation.tech/logo.png

# JWT secret for admin auth (sensitive)
JWT_SECRET=your_jwt_secret_here

# Notes:
# - After filling values, restart the backend server.
# - If you prefer Gmail OAuth2 instead of app password, tell me and I can implement it.
